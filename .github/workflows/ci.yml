name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check:
    name: Check
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7
        with:
          path: pi-subagent
      - uses: actions/checkout@v7
        with:
          repository: vegardx/pi-web
          ref: e05451d9ff1fcb5d0b306f09c0c759efc0a86940
          path: pi-web
      - uses: actions/setup-node@v7
        with:
          node-version: 24.16.0
          cache: npm
          cache-dependency-path: |
            pi-subagent/package-lock.json
            pi-web/package-lock.json
      - run: npm ci
        working-directory: pi-web
      - run: npm run build
        working-directory: pi-web
      - run: npm ci
        working-directory: pi-subagent
      - run: npm run check
        working-directory: pi-subagent
      - run: npm audit --audit-level=low
        working-directory: pi-subagent
